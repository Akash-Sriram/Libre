<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/libraryFragment">
    <fragment
        android:id="@+id/libraryFragment"
        android:name="app.libre.ui.fragments.LibraryFragment"
        android:label="fragment_library"
        tools:layout="@layout/fragment_library" />
    <fragment
        android:id="@+id/searchFragment"
        android:name="app.libre.ui.fragments.SearchSuggestionsFragment"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search_suggestions" />
    <fragment
        android:id="@+id/searchResultFragment"
        android:name="app.libre.ui.fragments.SearchResultFragment"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search_result">
        <argument
            android:name="query"
            app:argType="string"
            app:nullable="false" />
    </fragment>
    <fragment
        android:id="@+id/channelFragment"
        android:name="app.libre.ui.fragments.ChannelFragment"
        android:label="channel"
        tools:layout="@layout/fragment_channel">
        <argument
            android:name="channelId"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="channelName"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />
    </fragment>
    <fragment
        android:id="@+id/playlistFragment"
        android:name="app.libre.ui.fragments.PlaylistFragment"
        android:label="fragment_playlist"
        tools:layout="@layout/fragment_playlist">
        <argument
            android:name="playlistId"
            app:argType="string"
            app:nullable="false" />
        <argument
            android:name="playlistType"
            android:defaultValue="PUBLIC"
            app:argType="app.libre.enums.PlaylistType"
            app:nullable="false" />
    </fragment>
    <action
        android:id="@+id/openChannel"
        app:destination="@id/channelFragment" />
    <action
        android:id="@+id/openPlaylist"
        app:destination="@id/playlistFragment" />
    <action
        android:id="@+id/showSearchResults"
        app:destination="@id/searchResultFragment" />
    <action
        android:id="@+id/openSearch"
        app:destination="@id/searchFragment"
        app:popUpTo="@id/libraryFragment"
        app:popUpToSaveState="true"
        app:restoreState="false" />
</navigation>